export enum PublicConfig {
  // Base
  APP_NAME = 'APP_NAME',
  BACKEND_URL = 'BACKEND_URL',
  ENVIRONMENT = 'ENVIRONMENT',
  PORT = 'PORT',
  // Custom,
  HOSTNAME_DEFAULT = 'HOSTNAME_DEFAULT',
  HOSTNAME_ADMIN = 'HOSTNAME_ADMIN',
  SEO_PUBLIC_PATH = 'SEO_PUBLIC_PATH'
}

// API key, secret, token,...



export enum SecretConfig { }
